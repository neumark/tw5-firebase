setup:

npm install
./node_modules/.bin/firebase use --add peterneumark-com

keys:

A key.json file of the following format is required:

```
{
    "firebaseToken": ...,
    "apikey": ...,
    "refreshToken": ...,
}
```

firebaseToken can be generated by running:

    ./node_modules/.bin/firebase login:ci and save the resulting token as .firebase-token

apikey can be downloaded us "Browser Key" on https://console.developers.google.com/apis/credentials?pli=1&project=peterneumark-com&folder=&organizationId=

refreshToken can be gotten by logging into the wiki, and the running `firebase.auth().currentUser.refreshToken` in the JS console.


deploy:

./scripts/deploy.sh

local development:

# cd functions; ./run.sh
# python -m SimpleHTTPServer
# open http://localhost:8000/public/?host=http%3A%2F%2Flocalhost%3A5000%2Fpeterneumark-com%2Feurope-west3%2Fwiki-app%2Fpn-wiki%2F
